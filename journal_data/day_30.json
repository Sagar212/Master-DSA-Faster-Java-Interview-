{
  "day": 30,
  "patternType": "gr",
  "category": "\ud83d\udfe1 Graphs",
  "title": "\ud83d\udfe1 Graphs \u2014 Active Recall",
  "oneSentence": "Fill in the blanks for Graph traversals.",
  "problems": [
    {
      "code": [
        "class Solution {",
        "    public int numIslands(char[][] grid) {",
        "        int numIslands = 0;",
        "        for (int i = 0; i < grid.length; i++) {",
        "            for (int j = 0; j < grid[0].length; j++) {",
        "                if (grid[i][j] == '1') {",
        "                    [[blank1]];",
        "                    dfs(grid, i, j);",
        "                }",
        "            }",
        "        }",
        "        return numIslands;",
        "    }",
        "    ",
        "    private void dfs(char[][] grid, int i, int j) {",
        "        if (i < 0 || i >= grid.length || j < 0 || j >= grid[0].length || [[blank2]]) return;",
        "        [[blank3]];",
        "        dfs(grid, i + 1, j);",
        "        dfs(grid, i - 1, j);",
        "        dfs(grid, i, j + 1);",
        "        dfs(grid, i, j - 1);",
        "    }",
        "}"
      ],
      "blanks": [
        "numIslands++",
        "grid[i][j] == '0'",
        "grid[i][j] = '0'"
      ],
      "title": "Number of Islands"
    },
    {
      "code": [
        "class Solution {",
        "    public boolean canFinish(int numCourses, int[][] prerequisites) {",
        "        List<List<Integer>> adj = new ArrayList<>();",
        "        for (int i = 0; i < numCourses; i++) adj.add(new ArrayList<>());",
        "        int[] indegree = new int[numCourses];",
        "        for (int[] pre : prerequisites) {",
        "            adj.get(pre[1]).add(pre[0]);",
        "            [[blank1]];",
        "        }",
        "        Queue<Integer> q = new LinkedList<>();",
        "        for (int i = 0; i < numCourses; i++) {",
        "            if ([[blank2]]) q.offer(i);",
        "        }",
        "        int count = 0;",
        "        while (!q.isEmpty()) {",
        "            int curr = q.poll();",
        "            count++;",
        "            for (int next : adj.get(curr)) {",
        "                [[blank3]];",
        "                if (indegree[next] == 0) q.offer(next);",
        "            }",
        "        }",
        "        return [[blank4]];",
        "    }",
        "}"
      ],
      "blanks": [
        "indegree[pre[0]]++",
        "indegree[i] == 0",
        "indegree[next]--",
        "count == numCourses"
      ],
      "title": "Course Schedule"
    },
    {
      "code": [
        "class Solution {",
        "    public int orangesRotting(int[][] grid) {",
        "        Queue<int[]> q = new LinkedList<>();",
        "        int fresh = 0;",
        "        for (int i = 0; i < grid.length; i++) {",
        "            for (int j = 0; j < grid[0].length; j++) {",
        "                if ([[blank1]]) q.offer(new int[]{i, j});",
        "                else if (grid[i][j] == 1) fresh++;",
        "            }",
        "        }",
        "        if (fresh == 0) return 0;",
        "        int minutes = 0;",
        "        int[][] dirs = {{1,0},{-1,0},{0,1},{0,-1}};",
        "        while (!q.isEmpty()) {",
        "            int size = q.size();",
        "            boolean rotted = false;",
        "            for (int i = 0; i < size; i++) {",
        "                int[] curr = q.poll();",
        "                for (int[] d : dirs) {",
        "                    int r = curr[0] + d[0], c = curr[1] + d[1];",
        "                    if (r >= 0 && r < grid.length && c >= 0 && c < grid[0].length && [[blank2]]) {",
        "                        [[blank3]];",
        "                        q.offer(new int[]{r, c});",
        "                        [[blank4]];",
        "                        rotted = true;",
        "                    }",
        "                }",
        "            }",
        "            if (rotted) minutes++;",
        "        }",
        "        return [[blank5]];",
        "    }",
        "}"
      ],
      "blanks": [
        "grid[i][j] == 2",
        "grid[r][c] == 1",
        "grid[r][c] = 2",
        "fresh--",
        "fresh == 0 ? minutes : -1"
      ],
      "title": "Rotting Oranges"
    }
  ]
}