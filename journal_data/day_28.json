{
  "day": 28,
  "patternType": "gr",
  "category": "\ud83d\udfe1 Graphs",
  "title": "\ud83d\udfe1 Graphs \u2014 ROI & Pattern",
  "oneSentence": "Traverse networks using BFS for shortest paths and DFS for deep exploration.",
  "description": "Master the Graph traversal template. BFS uses a Queue, DFS uses recursion or a Stack. Both require a visited set.",
  "roi": {
    "frequency": "\u2b50\u2b50\u2b50\u2b50\u2b50 (Critical for onsite rounds)",
    "complexity": "Time: O(V+E) | Space: O(V)",
    "dependsOn": "Trees, Recursion, Queues",
    "signals": "Grids, networks, connected components, shortest paths."
  },
  "template": {
    "code": "class Solution {\n    public int numIslands(char[][] grid) {\n        if (grid == null || grid.length == 0) return 0;\n        int numIslands = 0;\n        for (int i = 0; i < grid.length; i++) {\n            for (int j = 0; j < grid[0].length; j++) {\n                if (grid[i][j] == '1') {\n                    numIslands++;\n                    dfs(grid, i, j);\n                }\n            }\n        }\n        return numIslands;\n    }\n    \n    private void dfs(char[][] grid, int i, int j) {\n        if (i < 0 || i >= grid.length || j < 0 || j >= grid[0].length || grid[i][j] == '0') {\n            return;\n        }\n        grid[i][j] = '0'; // mark as visited\n        dfs(grid, i + 1, j);\n        dfs(grid, i - 1, j);\n        dfs(grid, i, j + 1);\n        dfs(grid, i, j - 1);\n    }\n}",
    "steps": [
      {
        "line": 4,
        "explanation": "Iterate through each cell in the grid."
      },
      {
        "line": 6,
        "explanation": "When an unvisited land cell ('1') is found, increment the island count."
      },
      {
        "line": 8,
        "explanation": "Start a DFS traversal from this cell to mark all connected land cells as visited."
      },
      {
        "line": 15,
        "explanation": "In DFS, handle base cases: out of bounds or water/visited cell ('0')."
      },
      {
        "line": 18,
        "explanation": "Mark the current cell as '0' to avoid revisiting."
      },
      {
        "line": 19,
        "explanation": "Recursively visit all 4 adjacent directions (up, down, left, right)."
      }
    ],
    "sandbox": [
      {
        "q": "What happens if we don't mark cells as visited?",
        "options": [
          "The code runs faster.",
          "We get infinite recursion/StackOverflow.",
          "It counts more islands."
        ],
        "correct": 1,
        "explain": "Without marking cells as visited, the DFS will bounce back and forth between two adjacent '1's endlessly."
      },
      {
        "q": "Can we use BFS instead of DFS?",
        "options": [
          "Yes, using a Queue to visit level by level.",
          "No, BFS is only for shortest paths.",
          "Yes, but it takes O(V^2) time."
        ],
        "correct": 0,
        "explain": "Yes, BFS works perfectly fine. We just push the coordinates into a queue and process them iteratively."
      },
      {
        "q": "What is the time complexity?",
        "options": [
          "O(N)",
          "O(M * N) where M is rows, N is cols.",
          "O(M + N)"
        ],
        "correct": 1,
        "explain": "We potentially visit each cell in the M x N grid a constant number of times."
      }
    ],
    "followUps": [
      "How would you find the area of the largest island?",
      "What if the grid is too large to fit in memory?",
      "How to count islands if we can add land dynamically?"
    ]
  }
}