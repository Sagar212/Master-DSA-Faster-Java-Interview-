# Binary Search Pattern
Binary Search halves the search space at every step → O(log N). Works on sorted data or any monotonic search space.
## Variants: Standard, Boundary (first/last), Rotated Array, Search on Answer.
