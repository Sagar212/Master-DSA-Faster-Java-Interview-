# Variant Comparison: Trees / Graphs
## 1. DFS Preorder — Process node BEFORE children (root → left → right)
## 2. DFS Inorder — Process node BETWEEN children (left → root → right) — gives sorted BST
## 3. DFS Postorder — Process node AFTER children (left → right → root)
## 4. BFS — Level by level using Queue
