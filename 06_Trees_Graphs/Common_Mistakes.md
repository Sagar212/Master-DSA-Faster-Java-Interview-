# Common Mistakes: Trees / Graphs
1. **Not handling null** → Always check `if (root == null)` as the first line.
2. **Confusing DFS vs BFS** → DFS uses Stack/recursion, BFS uses Queue.
3. **Modifying the tree** → Be careful not to accidentally mutate node values during traversal.
