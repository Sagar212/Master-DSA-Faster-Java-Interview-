# Recall Notes: Trees / Graphs
- **Q: DFS vs BFS?** → DFS goes deep (stack/recursion). BFS goes wide (queue).
- **Q: When BFS over DFS?** → Level-order, shortest path (unweighted).
- **Q: LCA approach?** → DFS. If both subtrees return non-null, current node is LCA.
