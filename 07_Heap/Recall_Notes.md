# Recall Notes: Heap
- **Q: Kth largest — min or max heap?** → MIN-heap of size K. peek() = Kth largest.
- **Q: Java PriorityQueue default?** → MIN-heap. Use `Collections.reverseOrder()` for max.
- **Q: Merge K sorted lists approach?** → Min-heap of K heads. Poll smallest, add its next.
