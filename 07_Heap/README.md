# Heap Pattern
## Key: PriorityQueue in Java. MinHeap by default. Use `Collections.reverseOrder()` for MaxHeap.
## Variants: Top K (min-heap size K), Merge K Sorted (min-heap of heads), Running Median (two heaps).
